{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["tsconfig.base.json", "tsconfig.build.json"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "prebuild"],
      "outputs": ["dist/**", "src/.vitepress/dist/**"]
    },
    "prebuild": {
      "outputs": ["src/grammar-content.ts"]
    },
    "@gram-lang/docs#prebuild": {
      "dependsOn": ["^build"],
      "outputs": ["src/reference/api/parts/**", "src/public/llms-full.txt"]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["build"]
    }
  }
}
