{
  "name": "@gram/analyzer",
  "version": "0.9.0",
  "description": "Gram recipe analyzer for physical properties (mass, yield, nutrition)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@gram/parser": "file:../parser",
    "@gram/compiler": "file:../compiler",
    "@gram/i18n": "file:../i18n",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}