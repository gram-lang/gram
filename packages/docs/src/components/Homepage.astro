---
import Layout from "../layouts/Layout.astro";
import { Code } from "astro:components";
import Icon from "./Icon.astro";
import gramGrammar from "@gram-lang/parser/textmate";
import FeaturePanel from "./FeaturePanel.astro";
import { GRAM_VERSION } from "../lib/gram-version";
import { NUTRIENTS, type NutrientKey } from "@gram-lang/analyzer";
import { nutritionGroups } from "@gram-lang/renderer";
import { getDictionary } from "@gram-lang/i18n";

const { lang, t } = Astro.props;
const gramLang = { ...gramGrammar, name: "gram" } as any;
const codeThemes: Record<string, any> = {
	light: "github-light",
	dark: "github-dark",
};

// The nutrition panel mockup below is a static picture of what the real
// renderer produces — it is not rendered by it. Driving its labels and their
// grouping from `nutritionGroups()` keeps the two from drifting apart.
const nutritionDict = getDictionary(lang);

// Per-portion figures for the Chantilly mockup: one quarter of a 269 g batch
// (240 g cream + 25 g powdered sugar + 4 g vanilla), the recipe shown in
// `nutritionInput`. Values match the renderer's own output.
const MOCK_NUTRITION_VALUES: Partial<Record<NutrientKey, number>> = {
	calories: 231,
	fat: 22,
	carbs: 6.5,
	sugar: 6.3,
	fiber: 0,
	protein: 1.5,
	sodium: 11,
};

const mockNutrientGroups = nutritionGroups(MOCK_NUTRITION_VALUES as any, lang);

// The recipe declares `portions: 4`, so the real panel offers all three bases
// and opens on per-portion.
const mockBases = [
	{ label: nutritionDict.renderer.nutritionPerPortion, active: true },
	{ label: nutritionDict.renderer.nutritionPer100g, active: false },
	{ label: nutritionDict.renderer.nutritionWholeRecipe, active: false },
];
---

<Layout title="Gram" description={t.metaDescription} currentLang={lang}>
  <!-- Hero Section -->
  <section class="section hero" aria-labelledby="hero-heading">
    <div class="homepage-container">
      <div class="hero-layout">
        <div class="hero-text">
          <h1 class="title" id="hero-heading">{t.heroTitle}</h1>
          <p class="subtitle">{t.heroSubtitle}</p>
          <p class="description">
            {t.heroDescription}
          </p>

          <div class="actions">
            <a
              href={lang === "fr"
                ? "/fr/docs/explanation/philosophy"
                : "/docs/explanation/philosophy"}
              class="button primary">{t.heroGetStarted}</a
            >
            <a
              href={lang === "fr" ? "/fr/play" : "/play"}
              class="button secondary">{t.heroPlayground}</a
            >
          </div>

          <div class="version-badge">
            <a href={lang === "fr" ? "/fr/docs/changelog" : "/docs/changelog"}
              >Version {GRAM_VERSION}</a
            >
          </div>
        </div>

        <div class="hero-code">
          <div class="code-block" role="region" aria-label={t.recipeFileName || "recipe.gram"}>
            <div class="code-header">
              <span class="filename">{t.recipeFileName || "recipe.gram"}</span>
            </div>
            <Code code={t.recipeCode} lang={gramLang} themes={codeThemes} />
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Philosophy -->
  <section
    class="section philosophy alt-bg"
    aria-labelledby="philosophy-heading"
  >
    <div class="homepage-container">
      <h2 id="philosophy-heading">{t.whyGram}</h2>
      {t.whyGramDesc && <p class="section-desc">{t.whyGramDesc}</p>}
      <div class="grid">
        {
          t.philosophy.map(
            (item: {
              title: string;
              details: string;
              icon?: string;
              link?: string;
            }) => (
              <div class="grid-item">
                <div class="item-header">
                  {item.icon && (
                    <Icon name={item.icon as any} size="1.4rem" class="icon" />
                  )}
                  <h3>{item.title}</h3>
                </div>
                <p>{item.details}</p>
                {item.link && (
                  <a href={item.link} class="learn-more-link">
                    {t.learnMore}
                    <span class="sr-only">: {item.title}</span>
                    <span aria-hidden="true"> &rarr;</span>
                  </a>
                )}
              </div>
            ),
          )
        }
      </div>
    </div>
  </section>

  <!-- Use Cases with Vanilla Tabs -->
  <section class="section use-cases" aria-labelledby="use-cases-heading">
    <div class="homepage-container">
      <h2 id="use-cases-heading">{t.discoverGram}</h2>
      <p class="section-desc">
        {t.discoverDesc}
      </p>

      <div class="modern-features-container">
        <!-- Content Area -->
        <div class="features-content">
          <!-- Feature: Nutrition -->
          <details class="feature-accordion" open>
            <summary class="feature-summary">
              <h3 class="feature-title">{t.tabNutritionTitle}</h3>
            </summary>
            <FeaturePanel
              id="feature-nutrition"
              title={t.tabNutritionTitle}
              desc={t.tabNutritionDesc}
              link={t.tabNutritionLink}
              learnMoreText={t.learnMore}
              inputCode={t.nutritionInput}
              outputCode={t.nutritionOutput}
              gramLang={gramLang}
              codeThemes={codeThemes}
            >
              <div class="modern-mockup-card nut-card-mock" role="region" aria-label={nutritionDict.renderer.nutrition}>
                <div class="mockup-steps">
                  <ol class="gram-steps" aria-label={lang === "fr" ? "Étapes de la recette Chantilly" : "Chantilly recipe steps"}>
                    <li>
                      <span class="action"
                        >{lang === "fr" ? "Fouetter" : "Whisk"}</span
                      >
                      {lang === "fr" ? "La " : ""}<strong
                        >{
                          lang === "fr" ? "crème liquide" : "Heavy cream"
                        }</strong
                      > (240g)
                      {lang === "fr" ? " et le " : " and "}
                      <strong
                        >{
                          lang === "fr" ? "sucre glace" : "powdered sugar"
                        }</strong
                      > (25g)
                      {
                        lang === "fr"
                          ? " jusqu'à obtenir des pics souples."
                          : " until soft peaks form."
                      }
                    </li>
                    <li>
                      <span class="action"
                        >{lang === "fr" ? "Incorporer" : "Fold"}</span
                      >
                      {
                        lang === "fr"
                          ? "Ajouter délicatement l'"
                          : "Gently incorporate "
                      }<strong
                        >{
                          lang === "fr"
                            ? "extrait de vanille"
                            : "vanilla extract"
                        }</strong
                      > (4g).
                    </li>
                  </ol>
                </div>
                <div class="nut-panel-mock" role="group" aria-label={nutritionDict.renderer.nutrition}>
                  <div class="nut-header-mock">
                    {nutritionDict.renderer.nutrition}
                    <span>{nutritionDict.renderer.estimateTooltip}</span>
                  </div>
                  <div class="nut-basis-switch-mock" role="group" aria-label={lang === "fr" ? "Base de calcul nutritionnel" : "Nutrition calculation basis"}>
                    {
                      mockBases.map((basis) => (
                        <span
                          class:list={["pill", { active: basis.active }]}
                          aria-current={basis.active ? "true" : undefined}
                        >
                          {basis.label}
                        </span>
                      ))
                    }
                  </div>
                  <div class="nut-grid-mock">
                    {
                      mockNutrientGroups.map((group) => (
                        <div class="nut-item-mock">
                          <span class="label">{group.label}</span>
                          <strong>
                            {group.value}{" "}
                            <span class="unit">{group.unit}</span>
                          </strong>
                          {group.children.length > 0 && (
                            <ul class="nut-item-children" aria-label={group.label}>
                              {group.children.map((child) => (
                                <li>
                                  <span class="label">{child.label}</span>
                                  <span class="value">
                                    {child.value} {child.unit}
                                  </span>
                                </li>
                              ))}
                            </ul>
                          )}
                        </div>
                      ))
                    }
                  </div>
                </div>
              </div>
            </FeaturePanel>
          </details>

          <!-- Feature: Schedule -->
          <details class="feature-accordion">
            <summary class="feature-summary">
              <h3 class="feature-title">{t.tabScheduleTitle}</h3>
            </summary>
            <FeaturePanel
              id="feature-schedule"
              title={t.tabScheduleTitle}
              desc={t.tabScheduleDesc}
              link={t.tabScheduleLink}
              learnMoreText={t.learnMore}
              inputCode={t.scheduleInput}
              outputCode={t.scheduleOutput}
              gramLang={gramLang}
              codeThemes={codeThemes}
            >
              <div
                class="modern-mockup-card timeline-mockup-card"
                role="region"
                aria-label={lang === "fr" ? "Aperçu de la planification et du rétro-planning" : "Schedule and timeline preview"}
              >
                <div class="sr-only">
                  {lang === "fr"
                    ? "Rétro-planning : Temps total 17 minutes, temps actif 7 minutes. Déroulé : 0 à 5m : Mise en place ; 5 à 15m : Ébullition ; 10 à 15m : Sauter ; 15 à 17m : Mélanger."
                    : "Timeline schedule: Total time 17 minutes, active time 7 minutes. Steps: 0 to 5m: Mise en place; 5 to 15m: Boiling; 10 to 15m: Sauté; 15 to 17m: Toss."}
                </div>
                <div aria-hidden="true">
                  <div class="gantt-header-mock">
                    <span
                      >{lang === "fr" ? "Temps Total" : "Total Time"}: <strong
                        >17m</strong
                      ></span
                    >
                    <span
                      >{lang === "fr" ? " · Actif" : " · Active"}: <strong>7m</strong
                      ></span
                    >
                  </div>
                  <div class="gantt-tracks-container">
                    <div class="gantt-track-mock" style="height: 120px;">
                      <div class="track-label-mock">
                        {lang === "fr" ? "Actions" : "Actions"}
                      </div>
                      <div class="track-content-mock">
                        <div
                          class="time-block-mock passive-mock"
                          style="width: 26%; left: 0; top: 8px; border: none;"
                        >
                          <span
                            style="position: absolute; left: calc(100% + 10px); color: var(--sl-color-white); font-size: 0.85rem; font-weight: 600; white-space: nowrap;"
                            >Mise en place</span
                          >
                        </div>
                        <div
                          class="time-block-mock active-mock"
                          style="width: 26%; left: 52%; top: 44px;"
                        >
                          <span
                            style="position: absolute; left: calc(100% + 10px); color: var(--sl-color-white); font-size: 0.85rem; font-weight: 600; white-space: nowrap;"
                            >{lang === "fr" ? "Sauter" : "Sauté"}</span
                          >
                        </div>
                        <div
                          class="time-block-mock active-mock"
                          style="width: 10%; left: 78%; top: 80px;"
                        >
                          <span
                            style="position: absolute; left: calc(100% + 10px); color: var(--sl-color-white); font-size: 0.85rem; font-weight: 600; white-space: nowrap;"
                            >↳ {lang === "fr" ? "Mélanger" : "Toss"}</span
                          >
                        </div>
                      </div>
                    </div>
                    <div class="gantt-track-mock">
                      <div class="track-label-mock">
                        {lang === "fr" ? "Ébullition" : "Boiling"}
                      </div>
                      <div class="track-content-mock">
                        <div
                          class="time-block-mock passive-mock"
                          style="width: 52%; left: 26%;"
                        >
                          <span class="block-label-mock">10m</span>
                        </div>
                      </div>
                    </div>
                    <div
                      class="gantt-track-mock"
                      style="height: 36px; border-bottom: none;"
                    >
                      <div class="track-label-mock" style="border-right: none;">
                      </div>
                      <div class="track-content-mock" style="position: relative;">
                        <div class="gantt-tick" style="left: 0%;">0m</div>
                        <div class="gantt-tick" style="left: 26%;">5m</div>
                        <div class="gantt-tick" style="left: 52%;">10m</div>
                        <div class="gantt-tick" style="left: 78%;">15m</div>
                        <div class="gantt-tick" style="left: 88%;">17m</div>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </FeaturePanel>
          </details>

          <!-- Feature: Modules -->
          <details class="feature-accordion">
            <summary class="feature-summary">
              <h3 class="feature-title">{t.tabModulesTitle}</h3>
            </summary>
            <FeaturePanel
              id="feature-modules"
              title={t.tabModulesTitle}
              desc={t.tabModulesDesc}
              link={t.tabModulesLink}
              learnMoreText={t.learnMore}
              inputCode={t.modulesInput}
              outputCode={t.modulesOutput}
              gramLang={gramLang}
              codeThemes={codeThemes}
            >
              <div class="modern-mockup-card rendered-recipe-mock" role="region" aria-label={lang === "fr" ? "Recette modulaire assemblée" : "Composed modular recipe"}>
                <!-- Section 1 : Base importée (inlinée et mise à l'échelle) -->
                <div class="rendered-section">
                  <div class="rendered-section-header">
                    <h4>{lang === "fr" ? "Pâte Sablée" : "Crust"}</h4>
                    <span class="rendered-module-badge">
                      📦 {lang === "fr" ? "bases/pate-sablee.gram" : "bases/shortcrust.gram"} &rarr; {lang === "fr" ? "pâte" : "crust"} (×0.5)
                    </span>
                  </div>
                  <ol class="gram-steps">
                    <li>
                      <span class="action">{lang === "fr" ? "Sabler" : "Mix"}</span>
                      {lang === "fr" ? "La " : "The "}<strong
                        >{lang === "fr" ? "farine" : "flour"}</strong
                      > (125g), {lang === "fr" ? "le " : ""}<strong
                        >{lang === "fr" ? "beurre" : "butter"}</strong
                      > (62.5g), {lang === "fr" ? "le " : ""}<strong
                        >{lang === "fr" ? "sucre glace" : "powdered sugar"}</strong
                      > (45g){lang === "fr" ? " et l'" : ", and "}<strong
                        >{lang === "fr" ? "eau" : "water"}</strong
                      > (17.5g).
                    </li>
                  </ol>
                </div>

                <!-- Section 2 : Recette hôte -->
                <div class="rendered-section">
                  <div class="rendered-section-header">
                    <h4>{lang === "fr" ? "Tarte Citron" : "Lemon Tart"}</h4>
                  </div>
                  <ol class="gram-steps" start="2">
                    <li>
                      <span class="action">{lang === "fr" ? "Foncer" : "Line"}</span>
                      {lang === "fr" ? "Le cercle à tarte avec la " : "The tart ring with "}<strong
                        >{lang === "fr" ? "pâte" : "&crust"}</strong
                      > (250g).
                    </li>
                    <li>
                      <span class="action">{lang === "fr" ? "Cuire" : "Bake"}</span>
                      {lang === "fr" ? "Au four pendant " : "Blind bake for "}<strong>18min</strong> {lang === "fr" ? "à " : "at "}<strong>175°C</strong>.
                    </li>
                    <li>
                      <span class="action">{lang === "fr" ? "Garnir" : "Fill"}</span>
                      {lang === "fr" ? "Verser le " : "Pour in the "}<strong
                        >{lang === "fr" ? "crémeux citron" : "lemon curd"}</strong
                      > (300g).
                    </li>
                  </ol>
                </div>
              </div>
            </FeaturePanel>
          </details>

          <!-- Feature: Scale -->
          <details class="feature-accordion">
            <summary class="feature-summary">
              <h3 class="feature-title">{t.tabScaleTitle}</h3>
            </summary>
            <FeaturePanel
              id="feature-scale"
              title={t.tabScaleTitle}
              desc={t.tabScaleDesc}
              link={t.tabScaleLink}
              learnMoreText={t.learnMore}
              inputCode={t.scaleInput}
              outputCode={t.scaleOutput}
              gramLang={gramLang}
              codeThemes={codeThemes}
            >
              <table class="modern-mockup-card tabular-mockup">
                <caption class="sr-only">
                  {lang === "fr" ? "Pourcentages du boulanger pour la recette" : "Baker's percentages for the recipe"}
                </caption>
                <thead>
                  <tr class="sr-only">
                    <th scope="col">{lang === "fr" ? "Ingrédient et poids" : "Ingredient and weight"}</th>
                    <th scope="col">{lang === "fr" ? "Pourcentage" : "Percentage"}</th>
                  </tr>
                </thead>
                <tbody>
                  <tr class="tab-row reference-row">
                    <td class="tab-name"
                      ><strong
                        >{lang === "fr" ? "Farine T65" : "Bread Flour"}</strong
                      > - 400g</td
                    >
                    <td class="tab-val tab-val-ref">100%</td>
                  </tr>
                  <tr class="tab-row reference-row">
                    <td class="tab-name"
                      ><strong
                        >{
                          lang === "fr" ? "Farine Complète" : "Whole Wheat"
                        }</strong
                      > - 100g</td
                    >
                    <td class="tab-val">25%</td>
                  </tr>
                  <tr class="tab-row">
                    <td class="tab-name"
                      ><strong>{lang === "fr" ? "Eau" : "Water"}</strong> - 375g</td
                    >
                    <td class="tab-val">75%</td>
                  </tr>
                  <tr class="tab-row">
                    <td class="tab-name"
                      ><strong>{lang === "fr" ? "Levain" : "Levain"}</strong> - 100g</td
                    >
                    <td class="tab-val">20%</td>
                  </tr>
                  <tr class="tab-row">
                    <td class="tab-name"
                      ><strong>{lang === "fr" ? "Sel" : "Salt"}</strong> - 10g</td
                    >
                    <td class="tab-val">2%</td>
                  </tr>
                </tbody>
              </table>
            </FeaturePanel>
          </details>
        </div>
      </div>

      <div class="try-playground-container">
        <a href={lang === "fr" ? "/fr/play" : "/play"} class="button primary">
          {t.tryPlaygroundBtn || "Try it in the Playground"}
        </a>
      </div>
    </div>
  </section>

  <!-- Get Involved -->
  <section
    class="section get-involved alt-bg"
    aria-labelledby="get-involved-heading"
  >
    <div class="homepage-container">
      <h2 id="get-involved-heading">{t.getInvolved}</h2>
      <div class="grid involved-grid">
        <div class="grid-item card">
          <h3>{t.readBookTitle}</h3>
          <p>
            {t.readBookDesc}
          </p>
          <a
            href={lang === "fr"
              ? "/fr/docs/explanation/philosophy"
              : "/docs/explanation/philosophy"}
            class="link-btn"
            >{t.readBookLink}<span class="sr-only">: {t.readBookTitle}</span><span aria-hidden="true"> &rarr;</span></a
          >
        </div>
        <div class="grid-item card">
          <h3>{t.contributeTitle}</h3>
          <p>
            {t.contributeDesc}
          </p>
          <a href="https://git.gram-lang.org/gram-lang/gram" class="link-btn"
            >{t.contributeLink}<span class="sr-only">: {t.contributeTitle}</span><span aria-hidden="true"> &rarr;</span></a
          >
        </div>
      </div>
    </div>
  </section>

  <!-- Getting Started -->
  <section
    class="section getting-started"
    aria-labelledby="getting-started-heading"
  >
    <div class="homepage-container">
      <h2 id="getting-started-heading" style="margin-bottom: 1rem;">
        {t.tryTitle}
      </h2>
      <p
        style="margin-bottom: 2rem; color: var(--sl-color-gray-3); font-size: 1.1rem; text-wrap: balance;"
      >
        {t.tryDesc}
      </p>
      <div class="terminal-mini" role="region" aria-label={lang === "fr" ? "Commande d'initialisation CLI" : "CLI initialization command"}>
        <code id="cli-init-cmd"
          ><span class="prompt" aria-hidden="true">$</span> npx @gram-lang/cli init</code
        >
        <button
          type="button"
          class="copy-cmd-btn"
          aria-label={lang === "fr" ? "Copier la commande d'initialisation dans le presse-papiers" : "Copy initialization command to clipboard"}
          data-umami-event="copy-cli-init-command"
        >
          <span class="copy-label" aria-hidden="true">Copy</span>
          <span class="sr-only" aria-live="polite" id="copy-status"></span>
        </button>
      </div>
    </div>
  </section>
</Layout>

<script>
  // Preview/JSON toggles and copy-to-clipboard
  import { trackEvent } from "../lib/umami";

  document.addEventListener("DOMContentLoaded", () => {
    // Preview/JSON Toggle Logic with ARIA tabs and keyboard support
    const featurePanels = document.querySelectorAll<HTMLElement>(".feature-panel");
    featurePanels.forEach((panel) => {
      const toggleBtns = Array.from(panel.querySelectorAll<HTMLButtonElement>(".pill-btn"));
      const viewContents = Array.from(panel.querySelectorAll<HTMLElement>(".view-content"));

      const switchTab = (btn: HTMLButtonElement) => {
        const viewType = btn.getAttribute("data-view");
        toggleBtns.forEach((b) => {
          const isActive = b === btn;
          b.classList.toggle("active", isActive);
          b.setAttribute("aria-selected", isActive ? "true" : "false");
          b.setAttribute("tabindex", isActive ? "0" : "-1");
        });

        viewContents.forEach((vc) => {
          const isTarget = vc.classList.contains(`${viewType}-view`);
          vc.classList.toggle("active", isTarget);
          if (isTarget) {
            vc.removeAttribute("hidden");
          } else {
            vc.setAttribute("hidden", "");
          }
        });

        trackEvent("homepage-feature-view", { feature: panel.id, view: viewType });
      };

      toggleBtns.forEach((btn, index) => {
        btn.addEventListener("click", () => switchTab(btn));

        btn.addEventListener("keydown", (e) => {
          let targetIndex = -1;
          if (e.key === "ArrowRight") {
            targetIndex = (index + 1) % toggleBtns.length;
          } else if (e.key === "ArrowLeft") {
            targetIndex = (index - 1 + toggleBtns.length) % toggleBtns.length;
          } else if (e.key === "Home") {
            targetIndex = 0;
          } else if (e.key === "End") {
            targetIndex = toggleBtns.length - 1;
          }

          if (targetIndex !== -1) {
            e.preventDefault();
            toggleBtns[targetIndex].focus();
            switchTab(toggleBtns[targetIndex]);
          }
        });
      });
    });

    // Copy Command Logic with Screen Reader feedback
    const copyBtn = document.querySelector<HTMLButtonElement>(".copy-cmd-btn");
    const copyLabel = copyBtn?.querySelector<HTMLElement>(".copy-label");
    const copyStatus = document.getElementById("copy-status");
    if (copyBtn && copyLabel) {
      const defaultText = copyLabel.textContent || "Copy";
      copyBtn.addEventListener("click", async () => {
        try {
          await navigator.clipboard.writeText("npx @gram-lang/cli init");
          copyLabel.textContent = "Copied";
          if (copyStatus) {
            copyStatus.textContent = document.documentElement.lang === "fr"
              ? "Commande copiée dans le presse-papiers"
              : "Command copied to clipboard";
          }
          setTimeout(() => {
            copyLabel.textContent = defaultText;
            if (copyStatus) copyStatus.textContent = "";
          }, 2000);
        } catch (err) {
          console.error("Failed to copy!", err);
        }
      });
    }
  });
</script>

<style>
  /* Ensure code blocks in feature cards stretch to full container height */
  :global(.feature-card .astro-code),
  :global(.code-snippet-box .astro-code) {
    height: 100%;
  }

  /* Base Container */
  .section {
    padding: 5rem 1.5rem;
  }
  .alt-bg {
    background-color: var(--sl-color-bg-nav);
  }
  .homepage-container {
    max-width: 1152px;
    margin: 0 auto;
    font-family: var(--sl-font);
    color: var(--sl-color-text);
  }

  /* Typography & Basics */
  h1,
  h2,
  h3,
  h4 {
    color: var(--sl-color-white);
    font-weight: 600;
    margin-bottom: 1.5rem;
    line-height: 1.2;
  }

  h2 {
    font-size: 2.2rem;
    position: relative;
    margin-bottom: 0.85rem;
  }

  /* Subtle color accent on headers */
  /* h2::before {
    content: "";
    position: absolute;
    left: 0;
    top: 10%;
    height: 80%;
    width: 4px;
    background-color: var(--sl-color-accent-high, var(--sl-color-text-accent));
    border-radius: 2px;
  } */

  pre,
  code {
    font-family: var(--sl-font-mono);
    font-size: 0.9em;
  }

  /* Syntax Highlighting overrides for hero/tabs */
  .comment {
    color: var(--sl-color-gray-3);
  }
  .keyword {
    color: #ff7b72;
    font-weight: 500;
  }
  .tag {
    color: #79c0ff;
  }

  /* Hero Section */
  .hero-layout {
    display: flex;
    flex-wrap: wrap;
    gap: 4rem;
    align-items: center;
  }

  .hero-text {
    flex: 1 1 400px;
  }

  .version-badge {
    margin-top: 1rem;
  }
  .version-badge a {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--sl-color-text);
    text-decoration: underline;
    text-decoration-color: var(--gram-orange);
    text-decoration-thickness: 2px;
    text-underline-offset: 3px;
  }

  .title {
    font-size: 4.5rem;
    margin: 0 0 0.5rem 0;
    font-weight: 800;
    letter-spacing: -0.02em;
  }

  .subtitle {
    font-size: 1.75rem;
    color: var(--sl-color-white);
    margin: 0 0 1rem 0;
    text-wrap: balance;
  }

  .description {
    font-size: 1.15rem;
    color: var(--sl-color-gray-3);
    margin: 0 0 2rem 0;
    line-height: 1.6;
    text-wrap: balance;
  }

  .actions {
    display: flex;
    gap: 1rem;
  }

  .button {
    display: inline-block;
    padding: 0.7rem 1.4rem;
    text-decoration: none;
    font-weight: 600;
    border: 1px solid transparent;
    transition: background-color 0.15s ease, color 0.15s ease, border-color 0.15s ease;
  }

  .button.primary {
    background-color: var(--sl-color-accent);
    color: #1a0d00;
    font-weight: 700;
  }
  .button.primary:hover {
    background-color: var(--gram-orange-light, #ff952b);
    color: #1a0d00;
  }

  .button.secondary {
    background-color: transparent;
    color: var(--sl-color-white);
    border-color: var(--sl-color-border);
  }
  :root[data-theme="light"] .button.secondary {
    color: var(--sl-color-text);
  }
  .button.secondary:hover {
    border-color: var(--sl-color-accent);
    background-color: var(--sl-color-bg-nav);
    color: var(--sl-color-white);
  }
  :root[data-theme="light"] .button.secondary:hover {
    color: var(--sl-color-text);
  }

  /* Code Block Styling */
  .hero-code {
    flex: 1 1 400px;
  }

  .code-block {
    border: 1px solid var(--sl-color-border);
    overflow: hidden;
    font-size: 1rem;
  }

  .code-header {
    background-color: var(--sl-color-gray-7);
    border-bottom: 1px solid var(--sl-color-border);
    padding: 0 1rem;
    height: 42px;
    display: flex;
    align-items: center;
  }

  .filename {
    color: var(--sl-color-white);
    font-family: var(--sl-font-mono);
    font-size: 0.85rem;
    font-weight: bold;
  }

  .code-block pre {
    margin: 0;
    padding: 1.5rem;
    overflow-x: auto;
    color: var(--sl-color-gray-1);
    line-height: 1.5;
  }

  /* Grid Layouts */
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 3rem;
  }

  .item-header {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    margin-bottom: 0.8rem;
  }
  .item-header :global(.icon) {
    color: var(--sl-color-text-accent);
  }
  .grid-item h3 {
    font-size: 1.3rem;
    margin-bottom: 0;
  }
  .grid-item p {
    color: var(--sl-color-text);
    font-size: 1rem;
    line-height: 1.6;
    margin-bottom: 1rem;
  }
  .learn-more-link {
    display: inline-block;
    font-weight: 600;
    font-size: 1rem;
    color: var(--sl-color-text);
    text-decoration: underline;
    text-decoration-color: var(--gram-orange);
    text-decoration-thickness: 2px;
    text-underline-offset: 3px;
  }

  .philosophy h3 {
    margin-top: 0;
  }

  .section.philosophy {
    border-block-start: 10px solid var(--sl-color-accent);
  }

  /* Cards for Get Involved */

  .section.get-involved h2 {
    margin-top: 0;
    margin-bottom: 2rem;
  }

  .card {
    background-color: var(--sl-color-bg);
    padding: 2rem;
    display: flex;
    flex-direction: column;
    border: 1px solid var(--sl-color-border);
  }
  .card h3 {
    margin-top: 0;
  }

  .card p {
    flex-grow: 1;
  }
  .link-btn {
    display: inline-block;
    font-weight: 500;
    margin-top: 1rem;
    color: var(--sl-color-text);
    text-decoration: underline;
    text-decoration-color: var(--gram-orange);
    text-decoration-thickness: 2px;
    text-underline-offset: 3px;
  }


  /* Mini terminal */
  .terminal-mini {
    background-color: var(--sl-color-bg);
    border: 1px solid var(--sl-color-border);
    margin-top: 1rem;
    padding: 1.5rem 5.5rem 1.5rem 2.5rem;
    font-size: 1.1rem;
    max-width: fit-content;
    position: relative;
  }
  .prompt {
    color: var(--sl-color-accent-high, var(--sl-color-text-accent));
    margin-right: 0.5rem;
  }

  .copy-cmd-btn {
    position: absolute;
    top: 1.3rem;
    right: 0.8rem;
    background: transparent;
    border: 1px solid var(--sl-color-border);
    color: var(--sl-color-gray-3);
    cursor: pointer;
    font-size: 0.75rem;
    padding: 0.2rem 0.6rem;
  }

  .copy-cmd-btn:hover {
    color: var(--sl-color-white);
    border-color: var(--sl-color-gray-4);
  }

  /* Split View Section */
  .section-desc {
    color: var(--sl-color-gray-3);
    margin-bottom: 3rem;
    font-size: 1.1rem;
    font-weight: 600;
    position: relative;
    margin-top: 0;
  }

  /* Modern Features Section */

  .section.use-cases h2,
  .section.use-cases .section-desc {
  }

  .section.use-cases .section-desc {
    margin-bottom: 1.5rem;
    margin-top: 0;
  }

  .section.use-cases .section-desc:after {
    display: none;
  }

  .modern-features-container {
    display: flex;
    flex-direction: column;
  }

  .features-content {
    display: flex;
    flex-direction: column;
    margin-block-end: 3rem;
  }

  .feature-accordion {
    border-top: 1px solid var(--sl-color-border);
    padding-block: 1.75rem;
  }

  .feature-accordion:last-child {
    border-bottom: 1px solid var(--sl-color-border);
  }

  .feature-summary {
    cursor: pointer;
  }

  .feature-summary:hover .feature-title {
    color: var(--gram-orange);
  }

  .feature-accordion[open] .feature-summary {
    margin-bottom: 1.75rem;
  }

  .feature-title {
    display: inline;
    font-size: 1.3rem;
    margin: 0;
  }

  /* Mockups */
  .modern-mockup-card {
  }

  .mockup-text {
    font-size: 1.1rem;
    margin-bottom: 1.5rem;
    color: var(--sl-color-white);
    line-height: 1.5;
  }

  .gram-steps {
    list-style-type: decimal;
    padding-inline: 3rem 2rem;
    color: var(--sl-color-white);
    line-height: 1.6;
  }
  .gram-steps li {
    margin-bottom: 0.8rem;
  }
  .gram-steps .action {
    font-weight: 700;
    text-transform: uppercase;
    font-size: 0.8em;
    margin-right: 6px;
    padding: 2px 5px;
    border: 1px solid var(--sl-color-border);
  }

  /* Nutrition Panel Mockup */
  .nut-panel-mock {
    background-color: var(--sl-color-bg-nav);
    padding: 0.75rem;
    border: 1px solid var(--sl-color-border);
    margin: 0rem 1rem 1rem 1rem;
  }
  .nut-header-mock {
    font-size: 1.1rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    display: flex;
    align-items: center;
    gap: 6px;
    color: var(--sl-color-white);
  }
  .nut-header-mock span {
    font-size: 0.6em;
    text-transform: uppercase;
    background-color: var(--sl-color-gray-3);
    padding: 2px 6px;
    font-weight: 700;
    color: var(--sl-color-bg);
  }
  /* Mirrors the real panel's basis switch (renderer/gram.css), which is a set
     of radio inputs there and a static picture here. */
  .nut-basis-switch-mock {
    display: inline-flex;
    gap: 2px;
    margin-bottom: 0.6rem;
    padding: 2px;
    background-color: var(--sl-color-gray-6);
  }
  .nut-basis-switch-mock .pill {
    font-size: 0.7em;
    font-weight: 600;
    padding: 4px 10px;
    white-space: nowrap;
    color: var(--sl-color-gray-2);
  }
  .nut-basis-switch-mock .pill.active {
    background-color: var(--sl-color-bg);
    color: var(--sl-color-white);
  }
  .nut-grid-mock {
    display: grid;
    gap: 6px;
    grid-template-columns: repeat(2, 1fr);
    grid-auto-rows: min-content;
  }
  @media (min-width: 480px) {
    .nut-grid-mock {
      grid-template-columns: repeat(3, 1fr);
    }
  }
  .nut-item-mock {
    display: flex;
    flex-direction: column;
    gap: 2px;
    background-color: var(--sl-color-bg);
    padding: 8px 10px;
    border: 1px solid var(--sl-color-hairline);
  }
  .nut-item-mock .label {
    font-size: 0.65em;
    text-transform: uppercase;
    color: var(--sl-color-gray-3);
    font-weight: 600;
  }
  .nut-item-mock strong {
    font-size: 1.05em;
    font-family: var(--sl-font-mono);
    color: var(--sl-color-white);
  }
  .nut-item-mock strong .unit {
    font-size: 0.85em;
    font-weight: 400;
    color: var(--sl-color-gray-3);
  }
  .nut-item-children {
    list-style: none;
    margin: 0;
    padding: 0px 0 0 0;
    display: flex;
    flex-direction: column;
    gap: 2px;
  }
  .nut-item-children li {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: 4px;
    font-size: 0.7em;
    color: var(--sl-color-gray-3);
  }
  .nut-item-children .label {
    font-size: 0.95em;
    text-transform: none;
    color: var(--sl-color-gray-3);
    font-weight: 400;
  }
  .nut-item-children .value {
    font-family: var(--sl-font-mono);
    font-weight: 600;
    color: var(--sl-color-gray-2);
    white-space: nowrap;
  }

  /* Gantt Timeline Mockup */
  .timeline-mockup-card {
    padding: 1rem 0;
  }
  .gantt-header-mock {
    display: flex;
    padding: 0 1.5rem 1rem 1.5rem;
    border-bottom: 1px solid var(--sl-color-border);
    font-size: 0.85rem;
    color: var(--sl-color-gray-3);
  }
  .gantt-header-mock strong {
    color: var(--sl-color-white);
  }
  .gantt-track-mock {
    display: flex;
    height: 48px;
    align-items: center;
    border-bottom: 1px solid var(--sl-color-border);
    padding-right: 1rem;
  }
  .track-label-mock {
    width: 80px;
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--sl-color-white);
    border-right: 1px solid var(--sl-color-border);
    height: 100%;
    display: flex;
    align-items: center;
    padding-left: 1.5rem;
  }
  .track-content-mock {
    flex-grow: 1;
    position: relative;
    height: 100%;
  }
  .gantt-tick {
    position: absolute;
    top: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    color: var(--sl-color-gray-3);
    font-size: 0.75rem;
    font-family: var(--sl-font-mono);
    transform: translateX(-50%);
  }
  .gantt-tick::before {
    content: "";
    width: 1px;
    height: 6px;
    background-color: var(--sl-color-border);
    margin-bottom: 4px;
  }
  .track-content-mock {
    margin-left: 0.5rem;
  }
  .time-block-mock {
    position: absolute;
    top: 8px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.75rem;
    font-weight: 600;
  }
  .active-mock {
    background-color: var(--sl-color-accent);
    color: var(--sl-color-white);
  }
  .passive-mock {
    background-color: var(--sl-color-gray-5);
    border: 1px solid var(--sl-color-accent);
    color: var(--sl-color-accent);
  }
  .block-label-mock {
    padding: 0 8px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  /* Tabular Mockup */
  .tabular-mockup {
    width: 100%;
    border-collapse: collapse;
    padding: 0;
    text-align: left;
  }

  .tabular-mockup tbody {
    display: table-row-group;
  }

  .tab-row {
    border-bottom: 1px solid var(--sl-color-border);
  }

  .tab-row:last-child {
    border-bottom: none;
  }

  .tabular-mockup td {
    padding: 1rem 1.5rem;
    vertical-align: middle;
  }

  .reference-row {
    background-color: var(--sl-color-bg);
  }

  .tab-name {
    color: var(--sl-color-white);
  }

  .tab-val {
    color: var(--sl-color-text-accent);
    font-family: var(--sl-font-mono);
    font-weight: bold;
    text-align: right;
  }

  .tab-val-ref {
    color: var(--sl-color-text);
  }

  /* Rendered Modular Recipe Mockup */
  .rendered-recipe-mock {
    padding: 1.25rem 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .rendered-section {
    border-left: 3px solid var(--sl-color-border);
    padding-left: 1rem;
  }

  .rendered-section:first-child {
    border-left-color: var(--sl-color-text-accent);
  }

  .rendered-section-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
  }

  .rendered-section-header h4 {
    font-size: 1.05rem;
    font-weight: 600;
    color: var(--sl-color-white);
    margin: 0;
  }

  .rendered-module-badge {
    font-size: 0.75rem;
    font-family: var(--sl-font-mono);
    color: var(--sl-color-gray-2);
    background-color: var(--sl-color-bg-nav);
    padding: 3px 8px;
    border: 1px solid var(--sl-color-border);
  }

  .rendered-recipe-mock .gram-steps {
    padding-inline: 1.25rem 0.5rem;
    margin-bottom: 0;
  }

  #cli-init-cmd {
    display: block;
  }

  /* Accessibility: Focus visible and reduced motion */
  .button:focus-visible,
  .copy-cmd-btn:focus-visible,
  .learn-more-link:focus-visible,
  .link-btn:focus-visible,
  .version-badge a:focus-visible {
    outline: 2px solid var(--sl-color-accent);
    outline-offset: 3px;
    border-radius: 2px;
  }

  .feature-summary:focus-visible {
    outline: 2px solid var(--sl-color-accent);
    outline-offset: 4px;
    border-radius: 4px;
  }

  @media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
      scroll-behavior: auto !important;
    }
  }
</style>
