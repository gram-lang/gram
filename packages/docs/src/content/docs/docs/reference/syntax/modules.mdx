---
title: "Module Imports"
description: "Import another .gram file as a reusable sub-component with @use, and how scaling, encapsulation, and the shopping list work across files."
---

import { Tabs, TabItem, CardGrid, Card, LinkCard } from '@astrojs/starlight/components';

A recipe can `@use` another `.gram` file as a reusable sub-component — a shortcrust pastry, a stock, a batch of egg whites — instead of copy-pasting its steps into every recipe that needs it.

```gram
---
title: 'Lemon Meringue Tart'
---

@use "./bases/pate-sablee.gram" as &pate

## Assembly

[Line] the tart ring with &pate{250g}.

[Fill] with @lemon-curd{300g}.
```

The imported recipe's steps are seamlessly inlined into the timeline: a base's resting time interleaves with the host's steps, and its raw ingredients merge directly onto the unified shopping list.

---

## Syntax

`@use` directives must be declared immediately after the frontmatter, before any cooking step — see [Document Structure](/docs/reference/syntax/document-structure#2-module-imports).

<Tabs>
  <TabItem label="Default binding">
    Binds the default exported component to `&name`:

    ```gram
    @use "./bases/pate-sablee.gram" as &pate
    ```
  </TabItem>

  <TabItem label="Destructuring">
    Binds multiple exported components from the same module simultaneously:

    ```gram
    @use "./bases/eggs.gram" as { &whites, &yolks }
    ```
  </TabItem>

  <TabItem label="Renaming">
    Renames an exported component to avoid naming collisions in the host recipe:

    ```gram
    @use "./bases/cream.gram" as { &cream as &pastry-cream }
    ```
  </TabItem>

  <TabItem label="Multi-word names">
    Wrap names containing spaces or special characters in `{}`:

    ```gram
    @use "./bases/poached-chicken.gram" as &poached chicken{}
    @use "./bases/poached-chicken.gram" as { &shredded meat{} as &chicken }
    ```
  </TabItem>

  <TabItem label="Retro-planning (~{...})">
    Anchors the module's timeline relative to the host recipe:

    ```gram
    @use "./bases/sourdough-starter.gram" as &starter ~{-2d}
    ```
  </TabItem>
</Tabs>

### Specifier Rules
* The specifier must end in `.gram`.
* Supported formats: relative path (`./`, `../`), project root (`@/`), or custom alias (`@alias/`).
* Paths are strictly confined to the project root containing `.gram/` — absolute paths or URLs are rejected.

---

## Project-Root & Aliased Paths

`./` and `../` resolve relative to the *importing file's directory*. For deeply nested recipes, Gram provides two clean alternatives:

<Tabs>
  <TabItem label="Project Root (@/)">
    `@/` always points to the project root (the directory containing `.gram/`), regardless of file depth:

    ```gram
    @use "@/bases/pate-sablee.gram" as &pate
    ```
  </TabItem>

  <TabItem label="Path Aliases (@alias/)">
    Configure path aliases in `.gram/config.yaml`:

    ```yaml
    # .gram/config.yaml
    paths:
      bases: ./shared/bases
      sauces: ./shared/sauces
    ```

    Then import directly using `@alias/`:

    ```gram
    @use "@bases/pate-sablee.gram" as &pate
    @use "@sauces/bechamel.gram" as &sauce
    ```
  </TabItem>
</Tabs>

---

## What a Module Exports

Only **section-level** intermediate variables (`->&` on a `## Section` header) are exported. Step-level intermediates (`->&` inside a step) remain private to the module. See [Intermediate Variables](/docs/reference/syntax/intermediate-variables).

```gram title="bases/poached-chicken.gram"
---
title: 'Poached Chicken & Broth'
---

## Shredded Chicken ->&meat
[Poach] The @whole chicken{1.5kg} with aromatics in water for ~{45min}. [Shred] the meat once cooled.

## Golden Broth ->&broth
[Strain] and [simmer] the cooking liquid for ~{15min} to concentrate flavor.
```

* **Default Export**: A module with a single section export binds that target when using `as &name`. A module with multiple exports defaults to the output of its *last* section.
* **Destructured Exports**: Use `as { &a, &b }` to target specific components.
* **No Implicit Re-exports**: If `sauce.gram` imports `&stock` from `stock.gram`, importing `sauce.gram` only provides `&sauce`, not `&stock`. Re-exporting must be explicit via a dedicated section.

---

## Scaling

Gram measures a module's own output from its ingredients — nothing to declare:

```gram title="bases/shortcrust.gram"
---
title: 'Shortcrust Pastry'
---

## Pastry
Mix @flour{300g} with @butter{200g}.
```

`shortcrust.gram` sums its own ingredients to 500g. When an importing recipe requests `&pate{250g}`, Gram scales every ingredient in it by `0.5` automatically — the ratio between what was asked for and what the module actually measures out to.

:::note[When the measurement is only an estimate]
If working out a module's mass depends on an estimated density or a default unit weight rather than an explicit mass, Gram emits an `ESTIMATED_MODULE_YIELD` diagnostic rather than treating the scale factor as exact.
:::

### Scaling Destructured Exports
When importing multiple components from the same module, each component is measured on its own — from its own section's ingredients, plus anything that section itself references. If different ratios are requested, the **largest ratio** applies to the entire module, and Gram reports the extra production:

```gram title="bases/poached-chicken.gram"
---
title: 'Poached Chicken & Broth'
---

## Shredded Meat ->&meat
[Poach] The @whole chicken{1.5kg} with aromatics. [Shred] the meat (yields ~500g).

## Golden Broth ->&broth
[Strain] and [simmer] the cooking liquid (yields ~1000g).
```

```gram
@use "./bases/poached-chicken.gram" as { &meat, &broth }

## Risotto
[Cook] The @arborio rice{400g} with the &broth{2000g}.
[Top] With the shredded &meat{500g}.
```

`&broth{2000g}` against a measured 1000g of broth requires a ×2 ratio; `&meat{500g}` against 500g of meat requires ×1. The larger ratio (×2) applies to the whole module, so meat comes out to 1000g produced against the 500g actually used — Gram reports that surplus via `MODULE_SURPLUS` rather than silently discarding it.

### Timers & Batches
**Cooking and resting times are never scaled.** Doubling a cookie dough does not double or halve the baking duration.

When a discrete count is requested against a module's measured mass (`&cookies{2}` against a module that measures out to 500g), Gram interprets the request as *2 full batches* and emits `MODULE_BATCH_INTERPRETATION`.

---

## Chained Imports & Transitivity

Modules can import other modules, creating a dependency chain:

```gram
recipe.gram ──▶ @use "./sauce.gram" ──▶ @use "./stock.gram"
```

* **Encapsulation**: `recipe.gram` interacts only with `&sauce`. Private steps and local variables of `stock.gram` remain isolated.
* **Unified Schedule & Shopping List**: Raw ingredients (carrots, aromatics, bones) and simmering steps from `stock.gram` automatically bubble up into the main recipe.

---

## Diamond Dependencies & Cycle Prevention

### Diamond Dependencies
When a menu imports multiple dishes sharing a common base (e.g. an entrée and a dessert both importing `pate-sablee.gram`):
* The shared base file is parsed **only once**.
* The compiler creates independent instances scaled to each dish's requirements.
* Raw ingredients (flour, butter) merge cleanly on the shopping list.

### Cycle Prevention
If Recipe A imports B which imports A, Gram detects the circular dependency and halts with a `MODULE_CYCLE` diagnostic displaying the full cycle path (`A -> B -> A`).

---

## Encapsulation

* **Section Scoping**: Relative quantities (`@water{70% @&flour}`) and ingredient checks resolve only within the module's own sections. Host recipes using identical ingredient names never cause conflicts.
* **Headless Modules**: A module without section headers (`##`) is isolated into its own dedicated section upon import — it never merges into the host's untitled section.

---

## Frontmatter Inheritance

Informational metadata (`title`, `description`, `author`, `tags`, `category`) belongs strictly to the host recipe — importing a `vegan` base does not make an egg-based host tart vegan.

**Exceptions:**
* `densities:` merges into the host (host values take precedence on conflict).
* Baker's percentage references (`*`) inside imported modules are stripped on import.

---

## Stocking an Import (`--stock`)

When a component is already on hand (bought ready-made or prepared ahead of time), pass `--stock` to the CLI:

```bash
gram shop dinner.gram --stock @bases/bouillon.gram
gram cook dinner.gram --stock @bases/bouillon.gram
```

* **Timeline**: Cooking and prep steps for stocked modules are omitted.
* **Shopping List**: Appears as a single purchasable item (e.g. `1 bouillon`) instead of raw ingredients.
* **Nutrition**: Exact caloric and macronutrient values are preserved.

Supported by all CLI commands resolving `@use` (`build`, `check`, `view`, `export`, `cook`, `print`, `watch`, `shop`, `scale`).

---

## Anchoring an Import in Time (`~{-2d}`)

When a fresh preparation must be started in advance (e.g. a sourdough starter or marinade), anchor it directly on the `@use` line:

```gram
@use "./bases/starter.gram" as &starter ~{-2d}

## Bread
[Knead] with &starter{150g}.
```

* An importer-level `~{...}` clause overrides any retro-planning header declared inside the base (`RETRO_PLANNING_OVERRIDE_SHADOWED`).
* Combining `--stock` with `~{...}` triggers a `STOCKED_RETRO_PLANNING_IGNORED` diagnostic.

---

## Editor & Language Server Support

The Gram Language Server resolves `@use` directives in real time:

* **Instant Diagnostics**: Cross-file validation updates live as you type.
* **Gantt & Live Preview**: Composed schedule reflects scaled times and dependencies.
* **Go to Definition**: Jumping to `&pate` opens the source module at the exact exported section.
* **Auto-completion**: Typing `@use "` completes `./`, `../`, `@/`, and configured `paths:` aliases.

---

## Related Documentation

<CardGrid>
  <LinkCard 
    title="How-To: Organize Modular Recipes" 
    description="Hands-on guide to splitting recipes, configuring aliases, multi-day scheduling, and --stock workflows." 
    href="/docs/how-to/organize-modular-recipes" 
  />
  <LinkCard 
    title="Document Structure" 
    description="Overview of frontmatter metadata, directives, sections, and overall file layout." 
    href="/docs/reference/syntax/document-structure" 
  />
  <LinkCard 
    title="CLI Reference" 
    description="Complete documentation for gram build, gram shop, and global CLI options." 
    href="/docs/reference/tooling/cli" 
  />
</CardGrid>