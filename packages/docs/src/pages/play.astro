---
import Layout from "../layouts/Layout.astro";
import GramPlayground from "../components/playground/GramPlayground.vue";
---

<Layout
  title="Gram Playground"
  description="Write Gram recipes in your browser and see the parsed AST, compiled JSON, and rendered output update live."
  currentLang="en"
>
  <div class="playground-wrapper">
    <GramPlayground client:only="vue" lang="en" />
  </div>
</Layout>

<style>
  .playground-wrapper {
    max-width: calc(1400px + 3rem);
    width: 100%;
    min-width: 0;
    box-sizing: border-box;
    margin: 0 auto;
    padding: 1.5rem 1rem;
    flex: 1;
    display: flex;
    flex-direction: column;
    /* Reserve the space GramPlayground.vue's .playground-workspace takes once
       hydrated, so the client:only island doesn't collapse to 0 height and
       shift the footer down when it mounts. */
    min-height: calc(100vh - 60px);
  }

  @media (max-width: 767px) {
    .playground-wrapper {
      min-height: calc(100dvh + 30px);
    }
  }
</style>
