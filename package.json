{
  "name": "gram",
  "devDependencies": {
    "@changesets/assemble-release-plan": "^6.0.10",
    "@changesets/cli": "^2.31.0",
    "@changesets/config": "^3.1.4",
    "@changesets/read": "^0.6.7",
    "@manypkg/get-packages": "^3.1.0",
    "tsup": "^8.5.1",
    "turbo": "^2.9.18"
  },
  "license": "GPL-3.0",
  "private": true,
  "packageManager": "bun@1.3.14",
  "scripts": {
    "postinstall": "bun run build",
    "build": "turbo run build",
    "dev": "turbo run start",
    "test": "bun test",
    "change": "changeset",
    "version-packages": "bun run generate-release-note.ts && changeset version"
  },
  "workspaces": [
    "packages/*"
  ]
}