{
  "name": "gram",
  "private": true,
  "license": "GPL-3.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "npm run build",
    "build": "echo '🏗️ Building i18n, Parser, Compiler & Analyzer...' && npm --prefix packages/i18n run build && npm --prefix packages/parser run build && npm --prefix packages/compiler run build && npm --prefix packages/analyzer run build && echo '✅ Core build complete.'",
    "build:playground": "echo '🎨 Building Playground...' && npm --prefix packages/playground run build && echo '✅ Playground build complete.'",
    "dev": "echo '🚀 Starting Playground...' && npm start -w @gram/playground -s",
    "test": "echo \"Error: no test specified\" && exit 1",
    "change": "changeset",
    "version-packages": "bun run generate-release-note.ts && changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0"
  }
}